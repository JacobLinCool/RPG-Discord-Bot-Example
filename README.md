# RPG Discord Bot Example

A discord bot served as an example in NTNU CSIE camp.

`npm i` then `npm run build`, and you are ready to go with `npm start`.

> Don't forget to set your bot token and id in `.env` file.
